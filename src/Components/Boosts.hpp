#pragma once

#include <Types/Stats.hpp>

namespace pokesim {
struct AtkBoost {
  types::Boost boost = 0;
};

struct DefBoost {
  types::Boost boost = 0;
};

struct SpaBoost {
  types::Boost boost = 0;
};

struct SpdBoost {
  types::Boost boost = 0;
};

struct SpeBoost {
  types::Boost boost = 0;
};
}  // namespace pokesim