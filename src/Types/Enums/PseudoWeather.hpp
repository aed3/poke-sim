#pragma once

#include <cstdint>

namespace pokesim::dex {
enum class PseudoWeather : std::uint8_t {
  // clang-format off
  NO_PSEUDO_WEATHER = 0,
  // clang-format on
};
}  // namespace pokesim::dex
