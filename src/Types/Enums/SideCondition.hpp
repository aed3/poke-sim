#pragma once

#include <cstdint>

namespace pokesim::dex {
enum class SideCondition : std::uint8_t {
  // clang-format off
  NO_SIDE_CONDITION = 0
  // clang-format on
};
}  // namespace pokesim::dex
